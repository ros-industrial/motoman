This INIT_ROS.JBI shoudl be called from another Yaskawa Job.

It will return to the caller when 'robot_disable' has been called.
 by setting some Input signals, or to give Mikado control over the robot.

It is possible to immediately return if a given output is not set.

When Mikado attempts to gain control through 'robot_enable' when ROS_READY is not set, there will be a MotoROS Error 5009 "ROS not ready".

When Mikado is in control of the robot and then instructed to release robot control, Output#890 will become true and this job will end.

Mikado releases control when executing a "Run Robot Program" task with program name "robot_disable".

It will try to regain control and re-start this job when the same task is run with program name "robot_enable".

